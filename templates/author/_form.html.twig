{{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form.name, { 'label': 'Имя' ,'attr': {'class': 'form-control'} }) }}
    {{ form_row(form.surname, { 'label': 'Фамилия' ,'attr': {'class': 'form-control'} }) }}
    {{ form_widget(form) }}
    <button  class="btn btn-outline-primary">{{ button_label|default('Сохранить') }}</button>
{{ form_end(form) }}

